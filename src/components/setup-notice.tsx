export function SetupNotice() { return <section className="mx-auto max-w-3xl rounded-2xl border border-amber-300 bg-amber-50 p-6"><h2 className="text-xl font-semibold">Connect your commerce stack</h2><p className="mt-2 text-stone-700">Copy <code>.env.example</code> to <code>.env.local</code>, then add the Shopify Storefront token and Sanity project details. The storefront is intentionally showing this safe setup state until credentials exist.</p></section>; }
