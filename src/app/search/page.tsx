import { ProductCard } from "@/components/product-card";
import { getProducts } from "@/lib/shopify";
export const metadata = { title: "Search" };
export default async function SearchPage({ searchParams }: { searchParams: Promise<{ q?: string | string[] }> }) { const { q } = await searchParams; const term = typeof q === "string" ? q : ""; const products = term ? await getProducts(24, term) : []; return <main className="mx-auto w-full max-w-7xl flex-1 px-5 py-16"><h1 className="text-4xl font-medium">Search</h1><form className="mt-8"><label htmlFor="q" className="sr-only">Search products</label><div className="flex gap-3"><input id="q" name="q" defaultValue={term} placeholder="Search products" className="min-w-0 flex-1 rounded-full border border-stone-300 bg-white px-5 py-3 outline-none focus:border-stone-800" /><button className="rounded-full bg-stone-950 px-6 text-white">Search</button></div></form>{term && <p className="mt-8 text-stone-600">{products.length} results for “{term}”</p>}<div className="mt-8 grid gap-x-5 gap-y-10 sm:grid-cols-2 lg:grid-cols-4">{products.map((product) => <ProductCard key={product.id} product={product} />)}</div></main>; }
