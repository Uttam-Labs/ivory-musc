import Link from "next/link";import {customerAccountFetch,encodeCustomerId} from "@/lib/customer-account/client";import {ORDERS_QUERY} from "@/lib/customer-account/queries";import {formatMoney} from "@/lib/format";import styles from "../../account.module.css";
type Order={id:string;name:string;processedAt:string;financialStatus:string;fulfillmentStatus:string;totalPrice:{amount:string;currencyCode:string}};type Data={customer:{orders:{nodes:Order[]}}};
export const metadata={title:"Orders | Ivory Muse"};export default async function OrdersPage(){const {customer}=await customerAccountFetch<Data>(ORDERS_QUERY,{first:50});return <><header className={styles.header}><div><p className={styles.eyebrow}>Order history</p><h1 className={styles.title}>Your orders</h1></div></header><div className={styles.orderList}>{customer.orders.nodes.length?customer.orders.nodes.map(order=><Link className={styles.order} href={`/account/orders/${encodeCustomerId(order.id)}`} key={order.id}><strong>{order.name}</strong><span>{new Intl.DateTimeFormat("en-AU",{dateStyle:"medium"}).format(new Date(order.processedAt))}</span><span className={styles.status}>{order.fulfillmentStatus.replaceAll("_"," ")}</span><span>{formatMoney(order.totalPrice)}</span></Link>):<div className={styles.card}><p className={styles.muted}>No orders found for this account.</p><Link href="/collections/shop">Start shopping</Link></div>}</div></>}
